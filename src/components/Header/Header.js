import React from "react";
import "./Header.css";

export const Header = props => (
  <div id="main-header-wrapper" className="text-center">
        <h1>Minecraft Memory Game!</h1>
        <h5>[try to click every character, without clicking one twice]</h5>
  </div>
);